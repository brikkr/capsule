

class CapsuleElement {
    /**
     * @param {name} name of the element.
     * @param {schema} dataset schema of the element.
     * @param {container} container destination of the element.
     * @param {access} access rules of the element.
     */
    constructor(name, schema, container, access){
        this.name = name;
        this.schema = schema;
        this.container = container;
        this.access = access;
    }
    create(){

    }
    update(){

    }
}